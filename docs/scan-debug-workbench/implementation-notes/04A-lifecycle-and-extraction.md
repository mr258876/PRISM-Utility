# 阶段 04A：生命周期与提取门槛证据报告

## 基线与范围

- 基线：`dev`，SHA `4e9b3458c3596f4f4b1f03a9018bf8d028486566`。Windows 10.0.26200 x64；.NET SDK 9.0.318，已安装 .NET 8 runtime，没有 .NET 8 SDK。
- 预检：`rg --files -g AGENTS.md . '..\'` 与 `git ls-files '*AGENTS.md'` 未发现适用的 `AGENTS.md`；`git ls-files` 确认真实入口为 `PRISM Utility.sln`、`PRISM Utility/PrismUtility.csproj`、`PRISM Utility.Core/PrismUtility.Core.csproj` 和 `PrismUtility.Core.Tests/PrismUtility.Core.Tests.csproj`。
- 本报告是阶段 04A 的首次授权产物，只记录调用／所有权现状、契约证据、切片门槛及验证结果。唯一新增文件为本报告；未修改生产代码、测试、issue 状态或其他阶段报告。
- 工作树已有 stage 01、02、03A 的脏文件（含未跟踪阶段文档），均保留，未把它们归属为本轮改动。03A 报告记载未实施 03B、03C，也未实施阶段 04（[03A 报告](03A-signal-inspection.md#明确待办与边界)）。
- 依据总说明，阶段 04A 仅评估；阶段 04B 需另行明确授权。[总说明](../README.md#7-交给-codex-的启动指令)第 189 行明确规定首次只指定 04A；[阶段文件](../04-lifecycle-and-extraction.md)第 15 至 55 行要求调用／所有权图、P4-02、逐片 READY/BLOCKED，不因源码契约测试通过就推断运行时安全。
- 当前 registry 仍把 `VM-001`、`UI-006`、`UI-007`、`VM-002` 标为开放风险；`VM-001 -> UI-006 -> UI-007` 仍是提取硬门槛，VM-002 仅允许补行为 characterization。[`docs/architecture/issues-and-remediation.md:103`](../../architecture/issues-and-remediation.md#L103) [同上:104](../../architecture/issues-and-remediation.md#L104) [同上:105](../../architecture/issues-and-remediation.md#L105) [同上:106](../../architecture/issues-and-remediation.md#L106)

## P4-01：旧入口 → 保留入口 → 命令／操作 → owner 与数据

| 旧入口／触发 | 当前保留入口 | 命令／动作 | owner、状态或数据；离开、返回、换设备、退出的边界 |
| --- | --- | --- | --- |
| Shell 导航进入 ScanDebug | `NavigationService.NavigateTo` 仍导航到原 Page；Frame 导航感知只调用 `INavigationAware`，不调用 `CleanupAsync`。[`PRISM Utility/Services/NavigationService.cs:93`](../../../PRISM%20Utility/Services/NavigationService.cs#L93) [`PRISM Utility/Contracts/Navigation/NavigationLifecycleDispatcher.cs:8`](../../../PRISM%20Utility/Contracts/Navigation/NavigationLifecycleDispatcher.cs#L8) | 页面切换本身不取消扫描，也不等同操作停止 | `ScanDebugPage` 配置 `NavigationCacheMode="Enabled"`，注入 singleton `ScanDebugViewModel`；Page transient、VM singleton。离开时 Page 可缓存；返回时可能复用缓存实例，但本轮未观察实际导航行为。[`PRISM Utility/Views/ScanDebugPage.xaml:15`](../../../PRISM%20Utility/Views/ScanDebugPage.xaml#L15) [`PRISM Utility/App.xaml.cs:126`](../../../PRISM%20Utility/App.xaml.cs#L126) [`PRISM Utility/App.xaml.cs:127`](../../../PRISM%20Utility/App.xaml.cs#L127) |
| Page `Loaded` | `OnLoaded` | VM/UI event 订阅、`AttachRuntimeBindings`、窗口 Activated handler 及设备绑定刷新 | Page handler 用 `_areViewModelEventsSubscribed` 防止重复订阅；VM 分别 guard runtime binding 和 workspace 订阅。[`PRISM Utility/Views/ScanDebugPage.xaml.cs:588`](../../../PRISM%20Utility/Views/ScanDebugPage.xaml.cs#L588) [`PRISM Utility/Views/ScanDebugPage.xaml.cs:653`](../../../PRISM%20Utility/Views/ScanDebugPage.xaml.cs#L653) [`PRISM Utility/ViewModels/ScanDebugViewModel.cs:3197`](../../../PRISM%20Utility/ViewModels/ScanDebugViewModel.cs#L3197) [`PRISM Utility/ViewModels/ScanDebugViewModel.cs:3222`](../../../PRISM%20Utility/ViewModels/ScanDebugViewModel.cs#L3222) |
| Page `Unloaded` | `OnUnloaded` | 取消 Page 指针交互、解绑 Page handler、释放 Page Canvas bitmap、await `ViewModel.DeactivateAsync()` | 普通构建中 VM 的 `DeactivateAsync` 先 await `Task.CompletedTask`，随后同步执行 RAW epoch 停用、streaming preview 结束、runtime/workspace 解绑、timer 取消、清 preview；不等待扫描／校准／对焦操作结束。[`PRISM Utility/Views/ScanDebugPage.xaml.cs:623`](../../../PRISM%20Utility/Views/ScanDebugPage.xaml.cs#L623) [`PRISM Utility/ViewModels/ScanDebugViewModel.cs:11362`](../../../PRISM%20Utility/ViewModels/ScanDebugViewModel.cs#L11362) 只有 `[PRISM_VISUAL_QA]` 条件构建才在解绑前额外 await `PrismVisualQaCaptureService.StopAsync(this)`。[`PRISM Utility/Views/ScanDebugPage.xaml.cs:629`](../../../PRISM%20Utility/Views/ScanDebugPage.xaml.cs#L629) |
| Connect／Disconnect／scan 命令 | VM 命令，经 `IScanDebugSessionCoordinator` 和 `ScannerDeviceSessionManager` 操作共享 session | Connect 创建 scan-debug owner；Disconnect 仅在已连接且 idle 时断开；start 建立与 motion producer token 链接的 `_scanCts`；显式 StopScan 取消该 CTS 并请求 `session.StopScanAsync`，非连接态快照也会取消它 | 设备 session manager 管理 `ScannerSessionOwner`、底层 USB lease、session disconnect/dispose/release；Page navigation/deactivate 不释放它。普通离开不等于停止操作。[`PRISM Utility.Core/Services/ScanDebugSessionCoordinator.cs:34`](../../../PRISM%20Utility.Core/Services/ScanDebugSessionCoordinator.cs#L34) [`PRISM Utility.Core/Services/ScanDebugSessionCoordinator.cs:71`](../../../PRISM%20Utility.Core/Services/ScanDebugSessionCoordinator.cs#L71) [`PRISM Utility.Core/Services/ScannerDeviceSessionManager.cs:878`](../../../PRISM%20Utility.Core/Services/ScannerDeviceSessionManager.cs#L878) [`PRISM Utility/ViewModels/ScanDebugViewModel.cs:5348`](../../../PRISM%20Utility/ViewModels/ScanDebugViewModel.cs#L5348) [`PRISM Utility/ViewModels/ScanDebugViewModel.cs:5385`](../../../PRISM%20Utility/ViewModels/ScanDebugViewModel.cs#L5385) |
| session snapshot／targets 变化 | VM 常驻 snapshot handler 加激活期间的 runtime handlers | Connected/Running 快照时若 DeviceId 不同仅更新 `_calibrationDeviceSessionId` 并递增 generation，然后切换 connected session；非 connected 快照才取消 scan/autofocus/auto-calibration/manual-focus/focus-mapping CTS、推进 generation、清 claims 并切回 discovery | VM 构造时订阅 singleton coordinator `SnapshotChanged`，没有对应 VM dispose/unsubscribe caller；断连由非 connected snapshot 路径传播。直接 connected-device replacement 的 CTS/claims/操作归属行为尚未验证，是未解除风险。[`PRISM Utility/ViewModels/ScanDebugViewModel.cs:2097`](../../../PRISM%20Utility/ViewModels/ScanDebugViewModel.cs#L2097) [`PRISM Utility/ViewModels/ScanDebugViewModel.cs:3441`](../../../PRISM%20Utility/ViewModels/ScanDebugViewModel.cs#L3441) [`PRISM Utility/ViewModels/ScanDebugViewModel.cs:3458`](../../../PRISM%20Utility/ViewModels/ScanDebugViewModel.cs#L3458) [`PRISM Utility/ViewModels/ScanDebugViewModel.cs:3473`](../../../PRISM%20Utility/ViewModels/ScanDebugViewModel.cs#L3473) |
| workspace／设置／操作执行 | singleton workspace/settings/session coordinator；VM 在 runtime attach 时订阅 workspace | Deactivate 解绑 workspace、session targets、transfer settings、runtime claims；snapshot handler 在 singleton VM 生命周期内接收设备状态 | App DI 将相关服务注册为 singleton。[`PRISM Utility/App.xaml.cs:73`](../../../PRISM%20Utility/App.xaml.cs#L73) [`PRISM Utility/App.xaml.cs:83`](../../../PRISM%20Utility/App.xaml.cs#L83) [`PRISM Utility/App.xaml.cs:89`](../../../PRISM%20Utility/App.xaml.cs#L89) [`PRISM Utility/App.xaml.cs:109`](../../../PRISM%20Utility/App.xaml.cs#L109) 退出调用的 session manager shutdown 属于应用级资源回收，不是 VM/page cleanup。[`PRISM Utility/App.xaml.cs:167`](../../../PRISM%20Utility/App.xaml.cs#L167) |
| Canvas 预览与排队绘制 | Page code-behind 的 `CanvasControl.Draw`、`EnsurePreviewBitmap`、`DisposePreviewBitmap` | 按 `PreviewFrame` version/尺寸创建 bitmap，卸载或布局空态时 Dispose；VM 属性变更使用 `DispatcherQueue.TryEnqueue` 更新布局、Invalidate 或 overlay | Page 拥有 CanvasBitmap；VM 拥有 PreviewFrame/缓冲来源；代码中可见排队 closure 未带 Page epoch/实例 generation。源码有释放顺序，但没有实机绘制与 unload 交错验证。[`PRISM Utility/Views/ScanDebugPage.xaml.cs:947`](../../../PRISM%20Utility/Views/ScanDebugPage.xaml.cs#L947) [`PRISM Utility/Views/ScanDebugPage.xaml.cs:956`](../../../PRISM%20Utility/Views/ScanDebugPage.xaml.cs#L956) [`PRISM Utility/Views/ScanDebugPage.xaml.cs:991`](../../../PRISM%20Utility/Views/ScanDebugPage.xaml.cs#L991) [`PRISM Utility/Views/ScanDebugPage.xaml.cs:1068`](../../../PRISM%20Utility/Views/ScanDebugPage.xaml.cs#L1068) |
| ROI/pan/manual focus pointer | Page 保存 pointer capture 与 drag/pan 标识；manual focus 命令／CTS 在 VM | release、cancel、capture-lost 结束 ROI/pan；Unloaded 与窗口失焦调用 `CancelPreviewVisualInteraction`；manual focus 按钮的 release/cancel/capture-lost 调 VM 的 `EndManualFocusHold` | Page 卸载清理 visual pointer；manual focus 自身属于 VM 任务，Page Unloaded 没有 `EndManualFocusHold` 调用。捕获丢失时有结束路径，但 native pointer 行为没有本轮 UIA 验证。[`PRISM Utility/Views/ScanDebugPage.xaml.cs:1279`](../../../PRISM%20Utility/Views/ScanDebugPage.xaml.cs#L1279) [`PRISM Utility/Views/ScanDebugPage.xaml.cs:1569`](../../../PRISM%20Utility/Views/ScanDebugPage.xaml.cs#L1569) [`PRISM Utility/Views/ScanDebugPage.xaml.cs:1590`](../../../PRISM%20Utility/Views/ScanDebugPage.xaml.cs#L1590) [`PRISM Utility/Views/ScanDebugPage.xaml.cs:1695`](../../../PRISM%20Utility/Views/ScanDebugPage.xaml.cs#L1695) [`PRISM Utility/ViewModels/ScanDebugViewModel.cs:5039`](../../../PRISM%20Utility/ViewModels/ScanDebugViewModel.cs#L5039) |
| 校准／profile 对话框 | Page 订阅 VM request event 并创建 `ContentDialog` | prompt/notice 事件 handler 存在时，Page 的 `ShowAsync` 返回后完成 TCS，异常时 fault TCS；校准 prompt 和 notice 若无订阅 handler，VM 仍返回未完成 TCS task；profile discard/import confirmation 无 handler 时直接返回 `false` | 三种路径不能合并描述：prompt/notice 无 subscriber 会 pending；discard 两类 confirmation 无 handler 则拒绝确认并结束。dialog 已显示后卸载/关闭时 completion 是否终结仍未证实。[`PRISM Utility/Views/ScanDebugPage.xaml.cs:1000`](../../../PRISM%20Utility/Views/ScanDebugPage.xaml.cs#L1000) [`PRISM Utility/ViewModels/ScanDebugViewModel.cs:7353`](../../../PRISM%20Utility/ViewModels/ScanDebugViewModel.cs#L7353) [`PRISM Utility/ViewModels/ScanDebugViewModel.cs:7360`](../../../PRISM%20Utility/ViewModels/ScanDebugViewModel.cs#L7360) [`PRISM Utility/ViewModels/ScanDebugViewModel.cs:7385`](../../../PRISM%20Utility/ViewModels/ScanDebugViewModel.cs#L7385) [`PRISM Utility/ViewModels/ScanDebugViewModel.cs:7395`](../../../PRISM%20Utility/ViewModels/ScanDebugViewModel.cs#L7395) |
| 03A RAW row analysis | VM RAW partial + worker；VM 用 `_rawPageEpoch` 和 revision 校验 | streaming callback 先复制单行 packed bytes；最终数据 worker 使用所选行副本；结果回 UI dispatcher 前检查 page epoch、capture/pass/role/version/ROI revision | 当前 03A managed worker 防陈旧证据保留原 owner；测试还覆盖 deactivation 后 reattach 拒绝旧 callback，以及 dispatcher enqueue 被拒后行编辑重启 worker。没有 03B freeze、03C focus series。[`PRISM Utility/ViewModels/ScanDebugViewModel.RawSignal.cs:76`](../../../PRISM%20Utility/ViewModels/ScanDebugViewModel.RawSignal.cs#L76) [`PRISM Utility/ViewModels/ScanDebugViewModel.RawSignal.cs:167`](../../../PRISM%20Utility/ViewModels/ScanDebugViewModel.RawSignal.cs#L167) [`PRISM Utility/ViewModels/ScanDebugViewModel.RawSignal.cs:260`](../../../PRISM%20Utility/ViewModels/ScanDebugViewModel.RawSignal.cs#L260) [`PRISM Utility/ViewModels/ScanDebugViewModel.RawSignal.Worker.cs:9`](../../../PRISM%20Utility/ViewModels/ScanDebugViewModel.RawSignal.Worker.cs#L9) [`PrismUtility.Core.Tests/ScanDebugCalibrationStatusTests.cs:192`](../../../PrismUtility.Core.Tests/ScanDebugCalibrationStatusTests.cs#L192) [`PrismUtility.Core.Tests/ScanDebugCalibrationStatusTests.cs:256`](../../../PrismUtility.Core.Tests/ScanDebugCalibrationStatusTests.cs#L256) |
| Window close | `App.MainWindow_Closed` | 设置保存等待、session manager `ShutdownAsync`、debug mirror shutdown | 关闭 handler没有取得 ScanDebug VM、没有调用其 `DeactivateAsync` 或 `CleanupAsync`；共享 manager teardown 等待 active operation，断开 session、dispose session 并释放 USB lease。应用关闭的 VM 常驻订阅/其他 VM CTS cleanup 未由此证明。[`PRISM Utility/App.xaml.cs:167`](../../../PRISM%20Utility/App.xaml.cs#L167) [`PRISM Utility/App.xaml.cs:174`](../../../PRISM%20Utility/App.xaml.cs#L174) [`PRISM Utility/App.xaml.cs:185`](../../../PRISM%20Utility/App.xaml.cs#L185) [`PRISM Utility.Core/Services/ScannerDeviceSessionManager.cs:878`](../../../PRISM%20Utility.Core/Services/ScannerDeviceSessionManager.cs#L878) [`PRISM Utility.Core/Services/ScannerDeviceSessionManager.cs:1200`](../../../PRISM%20Utility.Core/Services/ScannerDeviceSessionManager.cs#L1200) |

跨页门槛不能由 ScanDebug 的局部 guard 解除：`ScanPage` 同样启用缓存；`ScanViewModel` 构造时 `Activate()`，Page `Loaded` 无 guard 地追加一次 `PropertyChanged` 订阅并再次激活，`Unloaded` 仅移除一次。VM 的 session 事件另有 guard，但 `CleanupAsync` 在生产代码中无调用者。[`PRISM Utility/Views/ScanPage.xaml:11`](../../../PRISM%20Utility/Views/ScanPage.xaml#L11) [`PRISM Utility/ViewModels/ScanViewModel.cs:461`](../../../PRISM%20Utility/ViewModels/ScanViewModel.cs#L461) [`PRISM Utility/Views/ScanPage.xaml.cs:35`](../../../PRISM%20Utility/Views/ScanPage.xaml.cs#L35) [`PRISM Utility/Views/ScanPage.xaml.cs:44`](../../../PRISM%20Utility/Views/ScanPage.xaml.cs#L44) [`PRISM Utility/ViewModels/ScanViewModel.cs:481`](../../../PRISM%20Utility/ViewModels/ScanViewModel.cs#L481) [`PrismUtility.Core.Tests/ScanPageLifecycleVm001CharacterizationTests.cs:29`](../../../PrismUtility.Core.Tests/ScanPageLifecycleVm001CharacterizationTests.cs#L29)

## P4-02：最小生命周期状态契约

| 契约／状态 | 支持的证据 | 仍不支持或待验证 |
| --- | --- | --- |
| 重复 Loaded 不重复 Page/VM runtime 订阅 | Page event guard、VM runtime/workspace guards 存在；source characterization 检查 guard 和 add/remove 对称。[`PRISM Utility/Views/ScanDebugPage.xaml.cs:653`](../../../PRISM%20Utility/Views/ScanDebugPage.xaml.cs#L653) [`PRISM Utility/ViewModels/ScanDebugViewModel.cs:3197`](../../../PRISM%20Utility/ViewModels/ScanDebugViewModel.cs#L3197) | 没有真实 Frame 连续进入／离开压力测试；VM constructor 对 coordinator 的 singleton SnapshotChanged 订阅不由 Deactivate 移除。 |
| 重复 Unloaded/Cleanup 不重复释放同一资源 | Page unsubscribe guard；VM raw deactivate 墂 epoch、runtime/workspace unsubscribe 有 guard；Canvas bitmap dispose 后置 null。[`PRISM Utility/Views/ScanDebugPage.xaml.cs:667`](../../../PRISM%20Utility/Views/ScanDebugPage.xaml.cs#L667) [`PRISM Utility/ViewModels/ScanDebugViewModel.cs:3211`](../../../PRISM%20Utility/ViewModels/ScanDebugViewModel.cs#L3211) [`PRISM Utility/ViewModels/ScanDebugViewModel.cs:3232`](../../../PRISM%20Utility/ViewModels/ScanDebugViewModel.cs#L3232) [`PRISM Utility/ViewModels/ScanDebugViewModel.RawSignal.cs:82`](../../../PRISM%20Utility/ViewModels/ScanDebugViewModel.RawSignal.cs#L82) | 重复生命周期在原生 WinUI host 上未运行验证；`CleanupAsync` 没有生产 caller。 |
| 旧 Deactivate 不解绑新会话 | `DeactivateAsync` 的普通生产构建路径在 `await Task.CompletedTask` 后同步执行解绑，没有真实未完成 await，因此当前代码证据**不支持存在 pending VM Deactivate 与 reentry 交错窗口**。[`PRISM Utility/ViewModels/ScanDebugViewModel.cs:11362`](../../../PRISM%20Utility/ViewModels/ScanDebugViewModel.cs#L11362) | 快速返回／重入仍未测试；`OnUnloaded` 有 `[PRISM_VISUAL_QA]` 条件 await，且旧 Page handler 的异步 continuation 没有 Page generation 防护。不能将其描述为已证明安全，也不能把一般 VM `DeactivateAsync` 误报为当前 pending race。 |
| 旧运行任务迟到结果不污染新设备／任务 | 非 connected snapshot 会取消 CTS；03A worker 再校验页面 epoch、revision、capture/pass/role/version。[`PRISM Utility/ViewModels/ScanDebugViewModel.cs:3473`](../../../PRISM%20Utility/ViewModels/ScanDebugViewModel.cs#L3473) [`PRISM Utility/ViewModels/ScanDebugViewModel.RawSignal.Worker.cs:21`](../../../PRISM%20Utility/ViewModels/ScanDebugViewModel.RawSignal.Worker.cs#L21) | Connected/Running deviceId 变化仅递增 generation 和切换 session，不在此分支取消任务；直接设备替换时完整运行任务的终止及迟到结果隔离尚未证明。普通页面导航不会取消扫描。 |
| 自动校准／自动对焦 CTS | VM 创建 auto-calibration CTS 并传给 session coordinator 的运行任务，finally dispose；autofocus CTS 链接 session connection 与 motion producer token，finally dispose。[`PRISM Utility/ViewModels/ScanDebugViewModel.cs:6310`](../../../PRISM%20Utility/ViewModels/ScanDebugViewModel.cs#L6310) [`PRISM Utility/ViewModels/ScanDebugViewModel.cs:6381`](../../../PRISM%20Utility/ViewModels/ScanDebugViewModel.cs#L6381) [`PRISM Utility/ViewModels/ScanDebugViewModel.cs:4915`](../../../PRISM%20Utility/ViewModels/ScanDebugViewModel.cs#L4915) [`PRISM Utility/ViewModels/ScanDebugViewModel.cs:4998`](../../../PRISM%20Utility/ViewModels/ScanDebugViewModel.cs#L4998) | 非 connected session 快照会 cancel CTS；Page deactivation 不 cancel；connected deviceId replacement 仅递增 generation，是否停止/替换中的操作归属未验证。 |
| 手动对焦／mapping test CTS | 手动对焦 CTS 链接 session/motion token，按钮 release/cancel/capture-lost 调 `EndManualFocusHold`；mapping-test CTS 链接相同 token，finally dispose。[`PRISM Utility/ViewModels/ScanDebugViewModel.cs:5029`](../../../PRISM%20Utility/ViewModels/ScanDebugViewModel.cs#L5029) [`PRISM Utility/ViewModels/ScanDebugViewModel.cs:5039`](../../../PRISM%20Utility/ViewModels/ScanDebugViewModel.cs#L5039) [`PRISM Utility/ViewModels/ScanDebugViewModel.cs:5117`](../../../PRISM%20Utility/ViewModels/ScanDebugViewModel.cs#L5117) [`PRISM Utility/ViewModels/ScanDebugViewModel.cs:10582`](../../../PRISM%20Utility/ViewModels/ScanDebugViewModel.cs#L10582) [`PRISM Utility/ViewModels/ScanDebugViewModel.cs:10643`](../../../PRISM%20Utility/ViewModels/ScanDebugViewModel.cs#L10643) | Page Unloaded 仅明确清理预览 ROI/pan 的 pointer capture，没有直接释放手动对焦按钮捕获或结束 VM hold；非 connected 快照会 cancel；native capture-lost 与离页安全未运行验证。 |
| 对话框卸载后等待完成 | Page dialog `ShowAsync` 正常返回后完成 TCS，异常时 fault TCS。[`PRISM Utility/Views/ScanDebugPage.xaml.cs:1014`](../../../PRISM%20Utility/Views/ScanDebugPage.xaml.cs#L1014) 校准 prompt/notice 无 subscriber 时 VM 返回 pending task；profile discard/import confirmation 无 handler 时直接 `false`。[`PRISM Utility/ViewModels/ScanDebugViewModel.cs:7353`](../../../PRISM%20Utility/ViewModels/ScanDebugViewModel.cs#L7353) [`PRISM Utility/ViewModels/ScanDebugViewModel.cs:7360`](../../../PRISM%20Utility/ViewModels/ScanDebugViewModel.cs#L7360) [`PRISM Utility/ViewModels/ScanDebugViewModel.cs:7395`](../../../PRISM%20Utility/ViewModels/ScanDebugViewModel.cs#L7395) | in-flight unload/close 的 completion 是否结束仍未证实；这些不同 request 分支不应统称为无 handler 都 pending。 |
| Canvas bitmap 与排队绘制 | Page 先解绑 VM events 再 dispose bitmap；draw 前校验 frame 与 bitmap。[`PRISM Utility/Views/ScanDebugPage.xaml.cs:632`](../../../PRISM%20Utility/Views/ScanDebugPage.xaml.cs#L632) [`PRISM Utility/Views/ScanDebugPage.xaml.cs:947`](../../../PRISM%20Utility/Views/ScanDebugPage.xaml.cs#L947) | 已排队的旧 Page dispatcher work 无 epoch token；没有 unload 与 draw 竞态实测。 |
| 后台 RAW 不读已归还/变化的数据 | 03A streaming 路径复制 packed row，worker pending 内容是行数组；最终 frame 由已有完成结果 owner 持有。[`PRISM Utility/ViewModels/ScanDebugViewModel.RawSignal.cs:167`](../../../PRISM%20Utility/ViewModels/ScanDebugViewModel.RawSignal.cs#L167) [`PRISM Utility/ViewModels/ScanDebugViewModel.RawSignal.cs:260`](../../../PRISM%20Utility/ViewModels/ScanDebugViewModel.RawSignal.cs#L260) managed 测试还覆盖 page deactivate 后 reattach 拒绝旧 workflow callback，以及 dispatcher 拒绝 publication 后 row edit 可重启 worker。[`PrismUtility.Core.Tests/ScanDebugCalibrationStatusTests.cs:192`](../../../PrismUtility.Core.Tests/ScanDebugCalibrationStatusTests.cs#L192) [`PrismUtility.Core.Tests/ScanDebugCalibrationStatusTests.cs:256`](../../../PrismUtility.Core.Tests/ScanDebugCalibrationStatusTests.cs#L256) | 本轮对应 filter **PASS 2/2**；仍不覆盖设备驱动真实 buffer 生命周期，也不是 WinUI 生命周期证明。不能外推至不存在的 freeze/baseline。 |
| 不在 UI 线程阻塞等待 | 本轮检查的对应清理路径使用 `await`；没有以 `.Wait()`/`.Result` 作为 Deactivate 顺序手段 | 不代表所有运行路径都完成异步 UIA 验证；此表不宣称整体 lifecycle 已修复。 |
| 应用关闭清理 | 关闭链路真实调用 `ScannerDeviceSessionManager.ShutdownAsync`；manager 等待 active operation 后断开 session、dispose session 并释放 USB lease。[`PRISM Utility/App.xaml.cs:185`](../../../PRISM%20Utility/App.xaml.cs#L185) [`PRISM Utility.Core/Services/ScannerDeviceSessionManager.cs:304`](../../../PRISM%20Utility.Core/Services/ScannerDeviceSessionManager.cs#L304) [`PRISM Utility.Core/Services/ScannerDeviceSessionManager.cs:878`](../../../PRISM%20Utility.Core/Services/ScannerDeviceSessionManager.cs#L878) [`PRISM Utility.Core/Services/ScannerDeviceSessionManager.cs:1200`](../../../PRISM%20Utility.Core/Services/ScannerDeviceSessionManager.cs#L1200) | 应用关闭不调用 ScanDebug `CleanupAsync`，不清 singleton VM coordinator 订阅，也不负责所有 VM/page 专属清理；保持为明确缺口，不宣称整体关闭生命周期已处理。 |

## P4-03：提取切片判定

`READY` 仅表示可独立作为纯 Core 行为切片被测试；不是提取授权，也不表示现在必须提取。涉及 Page、缓存 VM、UI host 或资源 owner 的项在硬门槛解除前均为 `BLOCKED`。

| 拟拆分职责 | 判定 | 证据、未满足条件与最小解除步骤 |
| --- | --- | --- |
| 已存在的 RAW 统计与纯范围计算 | **READY（仅已有 Core 纯计算可独立测试）** | `ScanRawSignalAnalyzer` 和 ROI/range 纯计算不持有 Page/VM lifecycle；当前专项与全量 Core 测试通过。无需为满足 04A 再提取或复制一层。[03A 报告](03A-signal-inspection.md#数据定义与并发边界) |
| lifecycle adapters、page activation host、VM scope/owner | **BLOCKED** | `VM-001` 和 `UI-006` 仍开放；singleton VM constructor snapshot subscription 与 window-close cleanup caller gap 未解决。最小步骤：明确 cached Page、singleton VM 及 app shutdown 的单一 owner/caller 契约，并补真实导航 leave/reentry/close 证据；再独立授权 04B 的一个切片。 |
| workspace/settings projection 面板 | **BLOCKED** | VM 仍负责 workspace subscription/projection 和公开 binding；卸载仅解绑现有订阅，未证明提取后 host 生命周期与重复激活。最小步骤：先解除 VM-001/UI-006 owner 门槛，再为绑定输入/命令和 attach/detach 行为补行为测试。 |
| 运行栏与命令状态展示 | **BLOCKED** | scan/calibration/focus/motion command、CTS、runtime claims、device/session generation 集中在 singleton VM。单纯展示属性也必须证明不会转移或二次拥有取消语义。最小步骤：先定义原 VM 唯一任务 owner 与无命令子控件契约，并测试取消/导航/换设备后旧回调隔离。 |
| Canvas 预览／ROI/pan/pointer／overlay | **BLOCKED** | `UI-007` 仍开放；Page 拥有 Win2D bitmap、绘制、capture、dispatcher work，VM 拥有 frame/ROI 状态；目前只有源码 characterization，非运行时证明。最小步骤：先解除 VM-001/UI-006；取得当前 Windows 原生 UIA 对 capture lost、卸载/重排和 draw/dispose 顺序的证据，再单独提取。 |
| 对话框、请求 completion | **BLOCKED** | Page handler 是 request event 的 TCS completion owner；卸载或 close 时取消/完成路径未证实。最小步骤：先补 dialog open -> cancel/close/unload completion 的可执行 host 测试及唯一 completion owner，再评估提取。 |
| workspace/tooling、复杂校准与 DNG 编排 | **BLOCKED** | VM-002 仍开放，VM 同时持有公开 command/event、会话编排、校准/自动对焦、profile projection、DNG coordination；拆分会碰共享 lifecycle/public binding。最小步骤：VM-001/UI-006/UI-007 阻塞链逐项解除并先固定行为回归；本次不拆。 |

## P4-T01..T10 验证矩阵

| ID | 本轮证据及结果 | 状态 |
| --- | --- | --- |
| P4-T01 连续进入／离开 | source tests 检查 subscribe guard/unsubscribe、workspace guard；本轮未运行 native WinUI 连续导航、dispatcher 残留或内存趋势验证。 | **NOT_RUN** 原生场景；managed/source 子集见命令 A/B |
| P4-T02 离开后迅速返回 | 已检查 Deactivate 普通构建没有真实异步 suspension；没有 native rapid reentry 测试，旧 Page handler generation guard 未见。 | **NOT_RUN** |
| P4-T03 运行中取消／导航／换设备 | managed coordinator、operation gate、session 生命周期及 VM characterization 有覆盖；不是一轮 native 页面导航/换设备交互，也不证明 navigation 会取消 operation。 | **PASS** managed/source 子集；native/device **NOT_RUN** / **NEEDS_DEVICE_VALIDATION** |
| P4-T04 dialog 打开时卸载／关闭 | source characterization 检查 ShowAsync completion/result 和异常路径；未运行 unload/close 下 completion 的 native test。 | **NOT_RUN** |
| P4-T05 ROI/pan/manual focus capture lost | geometry source tests 检查 pointer capture lost、cancel、release 路径；本轮没有真实 WinUI pointer/UIA，也没有设备手动对焦动作。 | **PASS** managed/source 子集；native **NOT_RUN**；真实运动 **NEEDS_DEVICE_VALIDATION** |
| P4-T06 Canvas draw 与释放重叠 | source tests 检查 Unloaded 解绑、dispose 顺序；App Release build 编译通过，不等于 Canvas 绘制运行检查。 | **PASS** source/build；native draw/dispose **NOT_RUN** |
| P4-T07 freeze/baseline/后台统计 | 03A row copy/worker、deactivate 后旧 callback 拒绝、dispatcher enqueue reject 后 row edit 重启 worker，本轮专项 2/2；03B freeze 与 baseline 未实施。 | **PASS** 仅现有 RAW managed 行为；freeze/baseline **NOT_RUN（未实施）**，设备 buffer 生命周期未验证 |
| P4-T08 应用退出 | 源码确认 MainWindow close 调 session manager Shutdown，但没有调用 ScanDebug VM Cleanup；未实际运行窗口关闭清理场景。 | **PASS** source chain；运行 **NOT_RUN** |
| P4-T09 command/host/workspace 回归 | 两组当前 managed/source 聚焦测试通过，含 command owner、Navigation host/workspace contract 与 session tests；应用 Release build 通过。 | **PASS** 命令 A/B 与 build |
| P4-T10 环境／证据分层 | Windows 可运行 managed tests、Core build 和 app build；SDK 9.0.318，机器无 .NET 8 SDK，但实际 `--no-restore` 命令成功。04A runtime harness 未执行，本轮没有 runtime verdict；也无设备验证。 | **PASS** 本轮已执行层级；04A runtime **NOT_RUN**，device **NEEDS_DEVICE_VALIDATION** |

## 实际测试命令与结果

环境：Windows 10.0.26200 x64，.NET SDK 9.0.318，.NET 8 runtime；没有 .NET 8 SDK。项目：`PrismUtility.Core.Tests/PrismUtility.Core.Tests.csproj`。命令 A/B 有重叠，数字不可相加。

**A. lifecycle / host / workspace 聚焦：PASS 65/65**

```text
dotnet test 'PrismUtility.Core.Tests/PrismUtility.Core.Tests.csproj' --no-restore -p:Platform=x64 --filter 'FullyQualifiedName~ScanPageLifecycleVm001CharacterizationTests|FullyQualifiedName~ScanWorkspaceUi006CharacterizationTests|FullyQualifiedName~ScanDebugGeometryUi007CharacterizationTests|FullyQualifiedName~ScanDebugViewModelVm002CharacterizationTests|FullyQualifiedName~ScannerLifecycleIntegrationRegressionTests|FullyQualifiedName~WorkspaceLifecycleSourceContractTests|FullyQualifiedName~NavigationDecisionBehaviorTests|FullyQualifiedName~ScannerSessionLifecycleContractsTests' -- RunConfiguration.MaxCpuCount=1
```

**B. session／operation／RAW／workflow 聚焦：PASS 154/154**

```text
dotnet test 'PrismUtility.Core.Tests/PrismUtility.Core.Tests.csproj' --no-restore -p:Platform=x64 --filter 'FullyQualifiedName~ScanDebugSessionCoordinatorTests|FullyQualifiedName~ScanDebugRuntimeOperationGateTests|FullyQualifiedName~ScanDebugCalibrationStatusTests.RawSignal_|FullyQualifiedName~ScanDebugCalibrationStatusTests.CaptureEvidence_|FullyQualifiedName~ScanRawSignalAnalyzerTests|FullyQualifiedName~ScanDebugRawSignalUiSourceTests|FullyQualifiedName~ScanWorkflowServiceTests|FullyQualifiedName~Log001DebugOutputMirrorLifecycleTests' -- RunConfiguration.MaxCpuCount=1
```

**C. RAW lifecycle callback 与 dispatcher reject 补充聚焦：PASS 2/2**

```text
dotnet test 'PrismUtility.Core.Tests/PrismUtility.Core.Tests.csproj' --no-restore -p:Platform=x64 --filter 'FullyQualifiedName~ScanDebugCalibrationStatusTests.RawSignal_WhenPageDeactivatesBeforeFinalCallback|FullyQualifiedName~ScanDebugCalibrationStatusTests.RawSignal_WhenWorkerPublicationIsRejected' -- RunConfiguration.MaxCpuCount=1
```

覆盖 `RawSignal_WhenPageDeactivatesBeforeFinalCallback_RejectsOldCaptureAfterReattach` 与 `RawSignal_WhenWorkerPublicationIsRejected_NextRowEditCanStartAnotherWorker`；managed VM/fake-dispatcher 证据，不是原生 WinUI 页面运行证据。

**D. 完整 managed Core suite：PASS 1449/1449**

```text
dotnet test 'PrismUtility.Core.Tests/PrismUtility.Core.Tests.csproj' --no-restore -p:Platform=x64 -- RunConfiguration.MaxCpuCount=1
```

**E. Core x64 build：PASS，0 warnings、0 errors**

```text
dotnet build 'PRISM Utility.Core/PrismUtility.Core.csproj' --no-restore -p:Platform=x64 -v:q
```

**F. WinUI app x64 Release build：PASS，0 errors、1 条既有 WindowsAppSDK PublishSingleFile advisory**

```text
dotnet build 'PRISM Utility/PrismUtility.csproj' --no-restore -p:Platform=x64 -p:Configuration=Release -m:1 -v:q
```

本轮没有执行 04A native runtime harness（导航／pointer／dialog／window close），也没有设备连接、采集或运动验证；记录为 **NOT_RUN**，不对 runtime 作通过或失败判断，也不归类为环境阻塞。设备相关实际行为标记 **NEEDS_DEVICE_VALIDATION**。不能将 Core 源码测试或应用 build 说成 WinUI runtime/视觉 PASS。
完整 `PRISM Utility.sln`（含原生 DNG bridge）构建 **NOT_RUN**：04A 未改原生代码，本轮以 Core 与 WinUI app 分层构建作为聚焦回归；03A 报告中的 C++ targets 缺失是历史结果，不能代替本轮的环境探测。

## 改动边界与最小后续步骤

- 本轮只新增本报告；没有改命令、持久化、session/USB lease、公开 binding 或生命周期行为；没有改 issue 状态，没有实现 04B、03B 或 03C，也没有运行硬件命令。
1. 单独授权并补当前 Windows native host 的四类导航场景：leave/reenter、rapid reentry、device replacement、window close；同时记录 dialog completion、queued dispatcher work 与 pointer capture lost。保持操作 owner 在原 VM/session manager，不以导航隐式取消运行操作。
2. 按职责补最小验证：校准 prompt/notice 与 discard confirmation 分别覆盖有/无订阅者及 dialog unload completion；scan/autofocus/autocalibration/manual-focus/mapping CTS 覆盖导航、断连及 connected deviceId 替换时的实际取消策略和旧回调归属（替换分支当前不取消 CTS）；session manager 覆盖断连/关闭等待 active operation、disconnect/dispose/lease release；RAW 保留现有两个 managed 用例，并另补真正的 native reentry/runtime 证据。
3. 基于上述证据明确 Page cache（返回可能复用，需运行确认）、singleton VM permanent snapshot subscription、VM cleanup caller 和 application shutdown 的唯一 owner/契约；完成相应 managed 行为测试与 native runtime 场景后，再更新提取门槛评估。
4. 仅在 registry blocker 经审查解除且用户另行明确授权 04B 的具体切片后，按纯计算、无生命周期参数面板、运行栏展示、最后预览/dialog/workspace 的顺序考虑单片提取；本报告不授权开始这些步骤。
