<Page
    x:Class="PRISM_Utility.Views.ShellPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:helpers="using:PRISM_Utility.Helpers"
    xmlns:behaviors="using:PRISM_Utility.Behaviors"
    xmlns:i="using:Microsoft.Xaml.Interactivity"
    Loaded="OnLoaded">

    <Grid>
        <Grid x:Name="AppTitleBar"
              Canvas.ZIndex="1"
              Height="{Binding ElementName=NavigationViewControl, Path=CompactPaneLength}"
              IsHitTestVisible="True"
              VerticalAlignment="Top">
            <Image Source="/Assets/WindowIcon.ico"
                   HorizontalAlignment="Left"
                   Width="16"
                   Height="16" />
            <TextBlock x:Name="AppTitleBarText"
                       VerticalAlignment="Center"
                       TextWrapping="NoWrap"
                       Style="{StaticResource CaptionTextBlockStyle}"
                       Margin="28,0,0,0"/>
        </Grid>
        <NavigationView
            x:Name="NavigationViewControl"
            Canvas.ZIndex="0"
            IsBackButtonVisible="Visible"
            IsBackEnabled="{x:Bind ViewModel.IsBackEnabled, Mode=OneWay}"
            SelectedItem="{x:Bind ViewModel.Selected, Mode=OneWay}"
            IsSettingsVisible="True"
            ExpandedModeThresholdWidth="1280"
            DisplayModeChanged="NavigationViewControl_DisplayModeChanged"
            Header="{x:Bind ViewModel.HeaderText, Mode=OneWay}">
            <NavigationView.FooterMenuItems>
                <NavigationViewItem
                    x:Name="ScannerConnectionItem"
                    Content="{x:Bind ViewModel.ScannerStatusShortText, Mode=OneWay}"
                    SelectsOnInvoked="False"
                    KeyDown="ScannerConnectionItem_KeyDown"
                    Tapped="ScannerConnectionItem_Tapped"
                    ToolTipService.ToolTip="{x:Bind ViewModel.ScannerStatusText, Mode=OneWay}"
                    AutomationProperties.Name="{x:Bind ViewModel.ScannerStatusText, Mode=OneWay}">
                    <NavigationViewItem.Icon>
                        <FontIcon FontFamily="{StaticResource SymbolThemeFontFamily}" Glyph="&#xECF0;" />
                    </NavigationViewItem.Icon>
                    <NavigationViewItem.InfoBadge>
                        <InfoBadge
                            x:Name="ScannerStatusBadge"
                            Width="16"
                            Height="16"
                            Background="{x:Bind ViewModel.ScannerStatusDotBrush, Mode=OneWay}"
                            Foreground="{x:Bind ViewModel.ScannerStatusBadgeForegroundBrush, Mode=OneWay}">
                            <InfoBadge.Resources>
                                <!-- Fix FontIcon margin -->
                                <Thickness x:Key="IconInfoBadgeFontIconMargin">4,1,4,1</Thickness>
                            </InfoBadge.Resources>
                        </InfoBadge>
                    </NavigationViewItem.InfoBadge>
                    <FlyoutBase.AttachedFlyout>
                        <Flyout x:Name="ScannerConnectionFlyout">
                            <StackPanel MinWidth="280" MaxWidth="360" Spacing="12">
                                <TextBlock
                                    Text="{x:Bind ViewModel.ScannerConnectionFlyoutTitle, Mode=OneWay}"
                                    Style="{ThemeResource SubtitleTextBlockStyle}" />
                                <StackPanel Spacing="4">
                                    <TextBlock
                                        Text="{x:Bind ViewModel.ScannerStatusText, Mode=OneWay}"
                                        TextWrapping="WrapWholeWords" />
                                    <TextBlock
                                        Text="{x:Bind ViewModel.ScannerConnectionFlyoutDescription, Mode=OneWay}"
                                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                        TextWrapping="WrapWholeWords" />
                                    <TextBlock
                                        Text="{x:Bind ViewModel.ScannerConnectionUnavailableText, Mode=OneWay}"
                                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                        TextWrapping="WrapWholeWords"
                                        Visibility="{x:Bind ViewModel.ScannerConnectionUnavailableVisibility, Mode=OneWay}" />
                                </StackPanel>
                                 <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Spacing="8">
                                     <Button x:Uid="Shell_ScannerConnectButton" Command="{x:Bind ViewModel.ConnectScannerCommand}" />
                                     <Button x:Uid="Shell_ScannerDisconnectButton" Command="{x:Bind ViewModel.DisconnectScannerCommand}" />
                                 </StackPanel>
                                <Button
                                    x:Uid="Shell_DeviceConfigurationButton"
                                    HorizontalAlignment="Stretch"
                                    Click="DeviceConfigurationButton_Click" />
                             </StackPanel>
                         </Flyout>
                    </FlyoutBase.AttachedFlyout>
                </NavigationViewItem>
            </NavigationView.FooterMenuItems>
            <NavigationView.MenuItems>
                <!--
                TODO: Update item titles by updating <x:Uid>.Content entries in Strings/en-us/Resources.resw.
                https://docs.microsoft.com/windows/uwp/app-resources/localize-strings-ui-manifest#refer-to-a-string-resource-identifier-from-xaml
            
                TODO: Update item icons by updating FontIcon.Glyph properties.
                https://docs.microsoft.com/windows/apps/design/style/segoe-fluent-icons-font#icon-list
                -->
                <NavigationViewItem x:Uid="Shell_Main" helpers:NavigationHelper.NavigateTo="PRISM_Utility.ViewModels.MainViewModel">
                    <NavigationViewItem.Icon>
                        <FontIcon FontFamily="{StaticResource SymbolThemeFontFamily}" Glyph="&#xE80F;"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem x:Uid="Shell_Log" helpers:NavigationHelper.NavigateTo="PRISM_Utility.ViewModels.LogViewModel">
                    <NavigationViewItem.Icon>
                        <FontIcon FontFamily="{StaticResource SymbolThemeFontFamily}" Glyph="&#xF0E3;"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem x:Uid="Shell_Scan" helpers:NavigationHelper.NavigateTo="PRISM_Utility.ViewModels.ScanViewModel">
                    <NavigationViewItem.Icon>
                        <FontIcon FontFamily="{StaticResource SymbolThemeFontFamily}" Glyph="&#xE8FE;"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem x:Uid="Shell_ScanDebug" helpers:NavigationHelper.NavigateTo="PRISM_Utility.ViewModels.ScanDebugViewModel">
                    <NavigationViewItem.Icon>
                        <FontIcon FontFamily="{StaticResource SymbolThemeFontFamily}" Glyph="&#xEC7A;"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.MenuItems>
            <NavigationView.HeaderTemplate>
                <DataTemplate>
                    <Grid>
                        <TextBlock
                            Text="{Binding}"
                            Style="{ThemeResource TitleTextBlockStyle}" />
                    </Grid>
                </DataTemplate>
            </NavigationView.HeaderTemplate>
            <i:Interaction.Behaviors>
                <behaviors:NavigationViewHeaderBehavior
                    DefaultHeader="{x:Bind ViewModel.HeaderText, Mode=OneWay}">
                    <behaviors:NavigationViewHeaderBehavior.DefaultHeaderTemplate>
                        <DataTemplate>
                            <Grid>
                                <TextBlock
                                    Text="{Binding}"
                                    Style="{ThemeResource TitleTextBlockStyle}" />
                            </Grid>
                        </DataTemplate>
                    </behaviors:NavigationViewHeaderBehavior.DefaultHeaderTemplate>
                </behaviors:NavigationViewHeaderBehavior>
            </i:Interaction.Behaviors>
            <Grid Margin="{StaticResource NavigationViewPageContentMargin}">
                <Frame x:Name="NavigationFrame" />
            </Grid>
        </NavigationView>
    </Grid>
</Page>
