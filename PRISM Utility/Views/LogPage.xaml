<Page
    x:Class="PRISM_Utility.Views.LogPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Page.Resources>
        <Thickness x:Key="LogCardPadding">12</Thickness>
        <CornerRadius x:Key="LogCardCornerRadius">8</CornerRadius>
        <Thickness x:Key="LogCardBorderThickness">1</Thickness>
        <x:Double x:Key="LogHeaderSpacing">6</x:Double>
        <x:Double x:Key="LogPanelSpacing">12</x:Double>
        <x:Double x:Key="LogQuietOpacity">0.8</x:Double>
        <Style x:Key="LogSectionCardStyle" TargetType="Border">
            <Setter Property="BorderBrush" Value="{ThemeResource CardStrokeColorDefaultBrush}" />
            <Setter Property="BorderThickness" Value="{StaticResource LogCardBorderThickness}" />
            <Setter Property="CornerRadius" Value="{StaticResource LogCardCornerRadius}" />
            <Setter Property="Padding" Value="{StaticResource LogCardPadding}" />
        </Style>
    </Page.Resources>

    <Grid
        Margin="{StaticResource SmallLeftRightMargin}"
        RowSpacing="{StaticResource LogPanelSpacing}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="1" Style="{StaticResource LogSectionCardStyle}">
            <Grid ColumnSpacing="{StaticResource LogPanelSpacing}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock
                    Grid.Column="0"
                    Text="{x:Bind ViewModel.StatusText, Mode=OneWay}"
                    Style="{ThemeResource BodyStrongTextBlockStyle}"
                    TextWrapping="WrapWholeWords" />
                <Button
                    Grid.Column="1"
                    x:Uid="Log_ClearButton"
                    Command="{x:Bind ViewModel.ClearCommand}" />
            </Grid>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource LogSectionCardStyle}">
            <Grid>
                <ListView
                    ItemsSource="{x:Bind ViewModel.Entries, Mode=OneWay}"
                    SelectionMode="None">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <TextBlock
                                Text="{Binding Display}"
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                TextWrapping="WrapWholeWords" />
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
                <TextBlock
                    x:Uid="Log_EmptyState"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    TextWrapping="WrapWholeWords"
                    Visibility="{x:Bind ViewModel.EmptyStateVisibility, Mode=OneWay}" />
            </Grid>
        </Border>
    </Grid>
</Page>
